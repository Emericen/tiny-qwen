# The game engine (rl.poker.game, seat.py statics) is stdlib-only.
# The browser table (rl.poker.server) and model seats need:
fastapi>=0.115
uvicorn>=0.34
httpx>=0.27
# The trainer rewrite will need these on the pod (pins encode the 8/22 landmines):
transformers>=5.13
trl>=1.10
peft>=0.18
datasets>=4.0
accelerate>=1.10
vllm>=0.17
