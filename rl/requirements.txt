# The dealer and its tests need nothing beyond the repo's requirements.txt.
# The browser table (rl.game) needs:
fastapi>=0.115
uvicorn>=0.34
# The trainer and the GPU baselines need these (install on the pod, see pod.sh):
transformers>=5.13
trl>=1.10
peft>=0.18
datasets>=4.0
accelerate>=1.10
vllm>=0.17
