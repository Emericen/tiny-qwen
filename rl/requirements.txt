# The dealer, the browser table (rl.game), and their tests are stdlib-only.
# The trainer rewrite will need these on the pod (pins encode the 8/22 landmines):
transformers>=5.13
trl>=1.10
peft>=0.18
datasets>=4.0
accelerate>=1.10
vllm>=0.17
